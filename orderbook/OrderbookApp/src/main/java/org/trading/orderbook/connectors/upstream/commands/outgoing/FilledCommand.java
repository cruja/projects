package org.trading.orderbook.connectors.upstream.commands.outgoing;

import org.trading.orderbook.connectors.commands.IOrderCommand;

public class FilledCommand implements IOrderCommand {

    @Override
    public void process() {

    }
}
